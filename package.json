{
  "name": "resumify",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "npm run dev --prefix Resume-Frontend",
    "dev:frontend": "npm run dev --prefix Resume-Frontend",
    "dev:backend": "npm run dev --prefix Resume-Backend",
    "start:backend": "npm start --prefix Resume-Backend"
  }
}
